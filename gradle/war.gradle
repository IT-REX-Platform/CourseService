apply plugin: "war"

bootWar {
    mainClassName = "de.uni_stutgart.it_rex.course.CourseServiceApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
