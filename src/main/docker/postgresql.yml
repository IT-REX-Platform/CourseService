version: '2'
services:
  courseservice-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/CourseService/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=CourseService
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
